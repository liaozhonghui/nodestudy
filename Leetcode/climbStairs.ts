function climbStairs(n: number): number {
    let res: number[] = [1, 1];
    let res: number[] = [1, 1];

    let res: number[] = [1, 1];
    let res: number[] = [1, 1];
    let res: number[] = [1, 1];
    let res: number[] = [1, 1];
    let res: number[] = [1, 1];
    let res: number[] = [1, 1];
    let res: number[] = [1, 1];
    let res: number[] = [1, 1];

    
    for (let i: number = 1; i < n; i++) {
        res[i + 1] = res[i - 1] + res[i];
    }
    return res[n];
};
function climbStairs(n: number): number {
    let res: number[] = [1, 1];
    for (let i: number = 1; i < n; i++) {
        res[i + 1] = res[i - 1] + res[i];
    }
    return res[n];
};
function climbStairs(n: number): number {
    let res: number[] = [1, 1];
    for (let i: number = 1; i < n; i++) {
        res[i + 1] = res[i - 1] + res[i];
    }
    return res[n];
};
function climbStairs(n: number): number {
    let res: number[] = [1, 1];
    for (let i: number = 1; i < n; i++) {
        res[i + 1] = res[i - 1] + res[i];
    }
    return res[n];
};
function climbStairs(n: number): number {
    let res: number[] = [1, 1];
    for (let i: number = 1; i < n; i++) {
        res[i + 1] = res[i - 1] + res[i];
    }
    return res[n];
};
function climbStairs(n: number): number {
    let res: number[] = [1, 1];
    for (let i: number = 1; i < n; i++) {
        res[i + 1] = res[i - 1] + res[i];
    }
    return res[n];
};
function climbStairs(n: number): number {
    let res: number[] = [1, 1];
    for (let i: number = 1; i < n; i++) {
        res[i + 1] = res[i - 1] + res[i];
    }
    return res[n];
};
function climbStairs(n: number): number {
    let res: number[] = [1, 1];
    for (let i: number = 1; i < n; i++) {
        res[i + 1] = res[i - 1] + res[i];
    }
    return res[n];
};
function climbStairs(n: number): number {
    let res: number[] = [1, 1];
    for (let i: number = 1; i < n; i++) {
        res[i + 1] = res[i - 1] + res[i];
    }
    return res[n];
};
function climbStairs(n: number): number {
    let res: number[] = [1, 1];
    for (let i: number = 1; i < n; i++) {
        res[i + 1] = res[i - 1] + res[i];
    }
    return res[n];
};
function climbStairs(n: number): number {
    let res: number[] = [1, 1];
    for (let i: number = 1; i < n; i++) {
        res[i + 1] = res[i - 1] + res[i];
    }
    return res[n];
};
function climbStairs(n: number): number {
    let res: number[] = [1, 1];
    for (let i: number = 1; i < n; i++) {
        res[i + 1] = res[i - 1] + res[i];
    }
    return res[n];
};
function climbStairs(n: number): number {
    let res: number[] = [1, 1];
    for (let i: number = 1; i < n; i++) {
        res[i + 1] = res[i - 1] + res[i];
    }
    return res[n];
};